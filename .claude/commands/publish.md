Publish the gamecodex server package to npm.

Run this command:
```
cd "/Users/s/Documents/Personal/Game Dev/Projects/GameCodex/packages/server" && npm publish --access public
```

If it fails due to OTP/authentication, tell the user to run the command directly in their terminal since the npm browser auth flow requires an interactive session.
