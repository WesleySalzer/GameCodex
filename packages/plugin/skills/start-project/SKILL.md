---
description: Scaffold a new game project with GameCodex
---

Use the gamecodex `scaffold_project` and `generate_gdd` MCP tools to help the user start a new game project based on: "$ARGUMENTS".

First generate a GDD outline, then scaffold the project structure. Ask which engine they want to use if not specified (MonoGame, Godot, or Phaser).
